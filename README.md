# astrbot-plugin-affection

Features:

- `签到`
- `签到排行榜`
- `lp`

Notes:

- Data is stored in `astrbot_plugin_affection/state.json` under the AstrBot plugin data directory.
- Group chat only.
- Compatible with `aiocqhttp` / NapCatQQ.

Developed by:

- Codex
